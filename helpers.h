#pragma once

void print_results(const int length_options[], const int values[],
                   const int cuts[], int number_of_length_options,
                   int remainder, int max_value);

void sort(int *length_options, int *values, int number_of_length_options);
